<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@ViewData["Title"] - HighLand Coffee</title>

    <!-- Add CSS links -->
    <link rel="stylesheet" href="~/Content/bootstrap.min.css" />
    <link rel="stylesheet" href="https://netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Add script links -->
    <script src="~/Scripts/jquery.min.js"></script>
    <script src="~/Scripts/popper.min.js"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/Scripts/jquery.min.js"></script>
    <script src="~/Scripts/popper.min.js"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>
    <script src="~/Scripts/jquery-3.0.0.min.js"></script>
    <script src="~/Scripts/plugin.js"></script>
    <script src="~/Scripts/jquery.mCustomScrollbar.concat.min.js"></script>
    <script src="~/Scripts/custom.js"></script>
    
    <style>
        .bg-red-dark {
            background-color: #B22830 !important;
        }
        .logo-custom {
            width: 120px;
            height: auto;
        }

        .navbar-nav .nav-link {
            font-weight: bold;
            text-transform: capitalize;
        }

        .navbar-nav {
            align-items: center;
        }
            .navbar-nav .nav-item {
                margin: 0 10px;
            }
        .bg-custom-footer {
            background-color: #B22830;
        }
        .text-light {
            color: #ffffff !important;
        }
        .footer_section a {
            color: #ffffff;
            text-decoration: none;
        }

            .footer_section a:hover {
                color: #f0f0f0;
                text-decoration: none;
            }
        .footer_section .row > div {
            text-align: center;
        }
        .footer_section h6 {
            margin-bottom: 20px;
            font-weight: bold;
        }
        .footer_section p {
            margin-bottom: 10px;
        }
        .footer_section .row {
            justify-content: center;
            align-items: center;
        }
        .footer_section .container {
            padding: 20px 0;
        }

    </style>
</head>
<body>
    <!-- Header section -->
    <div class="header_section">
        <nav class="navbar navbar-expand-sm bg-red-dark navbar-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                    <img src="~/Content/img/White_logo800.png" alt="Logo" class="logo-custom" />
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mynavbar">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="mynavbar">
                    <ul class="navbar-nav mx-auto">
                        <li class="nav-item active">
                            <a class="nav-link font-weight-bold" href="@Url.Action("Index", "Home")">TRANG CHỦ</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link font-weight-bold" href="@Url.Action("ViewProduct", "Product")">MENU</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link font-weight-bold" href="@Url.Action("Index", "Store")">CỬA HÀNG</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link font-weight-bold" href="@Url.Action("ViewBlog", "Blog")">TIN TỨC</a>
                        </li>
                        @if (Session["User"] != null)
                        {
                            <li class="nav-item active">
                                <a class="nav-link font-weight-bold" href="@Url.Action("User_info", "Account")">TÀI KHOẢN</a>
                            </li>
                            <li class="nav-item active">
                                <a class="nav-link font-weight-bold" href="@Url.Action("ViewCart", "Cart")">GIỎ HÀNG</a>
                            </li>
                            <li class="nav-item active">
                                <a class="nav-link font-weight-bold" href="@Url.Action("Index", "Order")">ĐƠN HÀNG</a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item active">
                                <a class="nav-link font-weight-bold" href="@Url.Action("Login", "Account")">ĐĂNG NHẬP</a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </div>

    <!-- Main content area -->
    <div class="main-content">
        @RenderBody()
    </div>

    <!-- Footer section -->
    <div class="footer_section layout_padding">
        <footer class="text-center text-lg-start bg-custom-footer text-light">
            <section>
                <div class="container text-center text-md-start">
                    <div class="row mt-3">
                        <div class="col-md-3 col-lg-4 col-xl-3 mx-auto mb-4">
                            <h6 class="text-uppercase fw-bold mb-4">
                                <i class=""></i>Highlands Coffee®
                            </h6>
                            <p>
                                Highlands Coffee® được thành lập bắt nguồn từ tình yêu dành cho đất Việt cùng với cà phê và cộng đồng nơi đây
                            </p>
                        </div>
                        <div class="col-md-2 col-lg-2 col-xl-2 mx-auto mb-4">
                            <h6 class="text-uppercase fw-bold mb-4">GIỚI THIỆU</h6>
                            <p><a href="@Url.Action("Index", "Home")" class="text-reset">TRANG CHỦ</a></p>
                            <p><a href="@Url.Action("ViewProduct", "Product")" class="text-reset">MENU</a></p>
                            <p><a href="@Url.Action("Index", "Store")" class="text-reset">CỬA HÀNG</a></p>
                            <p><a href="@Url.Action("ViewBlog", "Blog")" class="text-reset">TIN TỨC</a></p>
                        </div>
                        <div class="col-md-4 col-lg-3 col-xl-3 mx-auto mb-md-0 mb-4">
                            <h6 class="text-uppercase fw-bold mb-4">LIÊN HỆ</h6>
                            <p><a href="#"><i class="fa fa-facebook" aria-hidden="true"></i> Facebook</a></p>
                            <p><a href="#"><i class="fa fa-twitter" aria-hidden="true"></i> Twitter</a></p>
                            <p><a href="#"><i class="fa fa-instagram" aria-hidden="true"></i> Instagram</a></p>
                        </div>
                    </div>
                </div>
                <div class="text-center p-4" style="background-color: #B22830;">
                    © 2018 Highlands Coffee. All rights reserved.
                </div>
            </section>
        </footer>
    </div>
</body>
</html>